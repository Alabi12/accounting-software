default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: postgres
  password: wolo

development:
  <<: *default
  database: ifrs_accounting_development

test:
  <<: *default
  database: ifrs_accounting_test

production:
  <<: *default
  database: ifrs_accounting_production
  username: ifrs_accounting
  password: <%= ENV['INVENTORY_APP_DATABASE_PASSWORD'] %>
